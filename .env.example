# Copy this file to `.env` and fill in your real key:
#   cp .env.example .env
# `.env` is git-ignored, so your real key won't be committed.

# Blueticks API key — create one in your Blueticks dashboard.
BLUETICKS_API_KEY=bt_live_your_key_here
